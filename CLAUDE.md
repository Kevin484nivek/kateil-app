# Claude Code

This project's agent manual is in **[AGENTS.md](./AGENTS.md)**. Read it first.

This `CLAUDE.md` exists only as a pointer — Claude Code auto-loads it, and then redirects you to `AGENTS.md`. The actual instructions (onboarding, work protocol, doc maintenance triggers, session-close protocol, workspace integration) live in `AGENTS.md`, which is the universal agent manual also read by Codex and other AI tools.

Do not duplicate content here. If you need to change agent instructions, edit `AGENTS.md`.
