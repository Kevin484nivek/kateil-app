# Development

## Setup local

```powershell
cd C:\Users\kevin\Documents\Projects\kateil-app
npm install
```

Configurar variables usando como referencia:

- `.env.example`
- `.env.supabase.dev.example`

No commitear `.env` ni secretos.

## Comandos

```powershell
npm run dev
npm run build
npm run lint
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed
```

## Flujo IA

Antes de implementar:

1. Leer `AGENTS.md`.
2. Leer `docs/00-project-brief.md`.
3. Leer `docs/kateil/source-of-truth-v1.md`.
4. Revisar `git status`.

